/// ReloraFeatures holds the app's screens and screen-level models. It
/// depends on every other ReloraKit target; nothing depends on it.
///
/// The app target enters through `RootView` (RootView.swift), which owns the
/// root gate, the navigation stack, the single sheet slot, and the toast layer.
public enum ReloraFeaturesModule {}
